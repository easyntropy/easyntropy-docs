{
  "name": "easyntropy-docs",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "dev": "yarn run dev:docs",
    "dev:docs": "vuepress dev ./src/docs/",
    "build": "rm -rf ./dist/ ; yarn run build:docs",
    "build:docs": "vuepress build ./src/docs/"
  },
  "dependencies": {},
  "devDependencies": {
    "@vuepress/bundler-vite": "^2.0.0-rc.20",
    "@vuepress/theme-default": "^2.0.0-rc.90",
    "ethers": "^6.15.0",
    "sass-embedded": "^1.89.2",
    "vuepress": "^2.0.0-rc.20"
  }
}
